export type UserRole = 'ADMIN' | 'MANAGER' | 'RECEPTION' | 'DOCTOR' | 'LAB' | 'VIEWER';

export interface UserProfile {
  uid: string;
  email: string;
  displayName: string;
  role: UserRole;
  createdAt: string;
}
